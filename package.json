{
  "name": "anarchy-buddy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "test-watch": "jest --watch --passWithNoTests",
    "build": "rimraf build && tsc && rollup -c rollup.config.js && cp package.json ./build/package.json && cp .env ./build/.env && cd build && npm install --only=production",
    "start": "node ./index.js",
    "start-dev": "node ./build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.927.0",
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0",
    "string-to-color": "^2.2.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.1",
    "jest": "^27.0.4",
    "rimraf": "^3.0.2",
    "rollup": "^2.51.1",
    "typescript": "^4.3.2"
  }
}
